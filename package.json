{
  "name": "dirs-diredawa",
  "version": "1.0.0",
  "description": "Digital Intercity and Ride Sharing System for Dire Dawa City",
  "private": true,
  "workspaces": [],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd web-app && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../web-app && npm install",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
